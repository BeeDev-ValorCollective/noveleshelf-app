# noveleshelf-app

npm i on download,
npx expo start to run.  Scan QR code and open the Expo Go app on device to view realtime updates on phone/device.  type w in terminal when server is running to open the web version.